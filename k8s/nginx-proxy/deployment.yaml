apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-proxy
  namespace: db-ops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-proxy
  template:
    metadata:
      labels:
        app: nginx-proxy
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 27017
              name: mongo
            - containerPort: 3306
              name: mariadb
          readinessProbe:
            tcpSocket:
              port: 27017
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: nginx-proxy-config
---
apiVersion: v1
kind: Service
metadata:
  name: peer-db-proxy
  namespace: db-ops
spec:
  selector:
    app: nginx-proxy
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
    - name: mariadb
      port: 3306
      targetPort: 3306
