apiVersion: v1
kind: Service
metadata:
  name: mariadb-2-nodeport
  labels:
    app.kubernetes.io/name: mariadb
    app.kubernetes.io/component: database
spec:
  type: NodePort
  selector:
    statefulset.kubernetes.io/pod-name: mariadb-2
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30097
